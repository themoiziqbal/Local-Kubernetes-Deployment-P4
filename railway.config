# Railway deployment configuration

# The backend will be deployed to Railway with the following settings:
# - Start command: python run_server.py
# - Environment variables needed:
#   - OPENAI_API_KEY (your OpenAI API key)
#   - DATABASE_PATH (path to SQLite database, defaults to data/tasks.db)

# Railway will automatically detect this as a Python application
# based on the presence of requirements.txt and .py files